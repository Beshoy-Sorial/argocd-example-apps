apiVersion: batch/v1
kind: Job
metadata:
  name: presync-job
spec:
  template:
    metadata: {}
    spec:
      restartPolicy: Never
      containers:
        - image: busybox
          name: presync-job
          command:
            - sleep
            - "10"
